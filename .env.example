# Monorepo environment overview
# Copy values into each app's local `.env` file as needed.
#
# - apps/codebay/.env
# - apps/blog/.env
# - apps/community/.env

# Supabase configuration (used by multiple apps)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- Local development: cross-app links ---
# So Blog/Community links stay on localhost, set in each app's .env:
#
# apps/codebay/.env:
#   NEXT_PUBLIC_SITE_URL=http://localhost:3000
#   NEXT_PUBLIC_BLOG_URL=http://localhost:3001
#   NEXT_PUBLIC_COMMUNITY_URL=http://localhost:3002
#
# apps/blog/.env:
#   NEXT_PUBLIC_SITE_URL=http://localhost:3001
#   NEXT_PUBLIC_MAIN_URL=http://localhost:3000
#
# apps/community/.env:
#   NEXT_PUBLIC_SITE_URL=http://localhost:3002
#   NEXT_PUBLIC_BLOG_URL=http://localhost:3001
#   NEXT_PUBLIC_MAIN_URL=http://localhost:3000
#
# Run: codebay on 3000, blog on 3001, community on 3002 (see each app's package.json scripts).
